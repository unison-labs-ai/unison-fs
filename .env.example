# Copy to .env and fill in real values. Never commit the filled-in .env.

# Required: your Unison API key (usk_live_... format)
UNISON_TOKEN=usk_live_YOUR_KEY_HERE

# Optional: override the API base URL (default shown)
UNISON_API_URL=https://api.unisonlabs.ai
